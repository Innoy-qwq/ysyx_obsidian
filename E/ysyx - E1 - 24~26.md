这一部分集中讨论库函数的使用.

首先是strcpy和malloc, 也是老熟人了, 这里不多做介绍. 进入习题

#### 习题

1、自己实现一个`strcpy`函数，尽可能简洁，按照本书的编码风格你能用三行代码写出函数体吗？

> 答: 代码如下. 诚实而言我没能实现三行代码, 这是Gemini指导下的结果, 利用了赋值语句作为右值. 

```c
#include <stdio.h>

// 函数原型应当是char *strcpy(char *dest，const char *src);
// 实现的效果是将src的字符串填入dest, 并返回dest的指针

char *strcpy(char *dest, const char *src)
{
	char *ret = dest;
	while((*dest++ = *src++) != '\0'); //这里利用了赋值语句作为右值
	return ret; 
} 

int main(void){
	char A[10];
	printf("%s", strcpy(A, "innoy"));
};
```

2、编一个函数，输入一个字符串，要求做一个新字符串，把其中所有的一个或多个连续的空白字符都压缩为一个空格。这里所说的空白包括空格、'\t'、'\n'、'\r'。例如原来的字符串是：

```
This Content hoho       is ok
        ok?

        file system
uttered words   ok ok      ?
end.
```
压缩了空白之后就是：

```
This Content hoho is ok ok? file system uttered words ok ok ? end.
```

实现该功能的函数接口要求符合下述规范：

```
char *shrink_space(char *dest, const char *src, size_t n);
```

各项参数和返回值的含义和`strncpy`类似。完成之后，为自己实现的函数写一个Man Page。

>答: 代码如下:

```c
#include <stdio.h>

// 函数原型应当是char *shrink_space(char *dest, const char *src, size_t n);
// 实现的效果是将src的字符串选取n个, 进行空格压缩并填入dest, 若字符串不满n个则后续填\0

char *shrink_space(char *dest, const char *src, size_t n)
{
    char *ret = dest;
    int flag = 0; // 表示是否已经出现空位;
    for (int count = 0; count < n; count++)
    {
        if (*src == ' '  ||
            *src == '\t' ||
            *src == '\n' ||
            *src == '\r'){
            if(!flag){
            *dest = ' ';
            dest++;
            flag = 1;}
            }
        else{
            *dest = *src;
            dest++;
            flag = 0;
            }

        if (*src != '\0')
            src++;
    }
    
    return ret;
}

int main(void)
{
    char a[30];
    printf("%s", shrink_space(a, "Hello \n \t Innoy", 15));
};
```

```
Man Page

NAME
	shrink_sapce
	
SYNOPSIS
	char *shrink_space(char *dest, const char *src, size_t n)
	
DESCRIPTION
	The shrink_space() function copies at most n characters from the string src  
	into the buffer dest, while compressing sequences of whitespace characters  
	(space, '\t', '\n', '\r') into a single space character.  
	  
	Whitespace compression is performed during copying:  
	if multiple consecutive whitespace characters are encountered in src,  
	they are replaced by a single space in dest.
	
RETURN VALUE  
	The function returns a pointer to dest.
	
NOTES  
	The caller is responsible for ensuring that dest has enough space to hold  
	at least n characters.
	
BUGS
	If the destination string of a shrink_space() is not large enough (that is,
	if the programmer was stupid or lazy,and failed to check the size
	before copying) then anything might happen. Overflowing fixed length
	strings is a favorite cracker technique.
```

### malloc and free

malloc申请内存, 返回void* 类型指针, 

>注意: malloc申请的内存被free掉, 只是这一块地址不再归属于用户程序, 指向这块地址的指针当然仍是存在的, 这时我们最好手动把这个野指针指向NULL来保证安全.

#### 习题: 

1、小练习：编写一个小程序让它耗尽系统内存。观察一下，分配了多少内存后才会出现分配失败？内存耗尽之后会怎么样？会不会死机？

>答: 代码如下, 代码的内存占用攀升, 计算机卡顿, 最终杀掉进程. 

```c
#include <stdio.h>

int main(int argc,char *argv[]){
    unsigned int size = 0;
    while (1){
        malloc(100000000);
        size+=100000000;
        printf("%d\n", size);
    }
}
```

### 传入参数与传出参数

传入参数就是作为指针参数进入函数后,读取其数据进行使用的参数. 传出函数就是作为指针参数进入函数后, 被写入数据用于外部读取的参数, value-result函数就是既作为传入读取也作为传出调用的指针.

>传出参数和result-value参数作为函数接口完全相同.


两层指针的用法应当很明白, 没什么额外的信息. 

### 回调函数

经典的回调函数, 函数指针作为参数被传入到函数中提供调用, 一般使用viod*.
C++的泛型算法（Generics Algorithm）中可以使用回调函数, 其作用是可以有一个灵活的比较函数, 外部调用者自行实现两个对象的比较函数, 然后将这个比较函数的函数指针作为回调函数传入就可以了. 这样的话可以实现比如结构体之间的比较. 

这里效果上就有点类似于cpp的运算符重载

#### 习题

1、K&R 的5.6节有一个`qsort`函数的实现，可以对一组任意类型的对象做快速排序。请读者仿照那个例子，写一个插入排序的函数和一个折半查找的函数。

>答: 代码如下

```c

```


### 可变参数

