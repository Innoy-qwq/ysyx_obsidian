#c
### 今日解决疑问:

为什么函数中使用`static char *next = str;`会有错误?

>static关键字的对象必须是编译期常量.

strtok有什么要注意的地方?

>这个函数对于原参数str是**破坏性**的. 这个函数并不会自发申请一块内存然后保存字符串, 对这个字符串做处理然后返回给你. 它做的是直接对原参数字符串进行破坏, 当你分割完, 原字符串也会被损毁. 

--- 

### 今日工作总结：

- 完成strtok