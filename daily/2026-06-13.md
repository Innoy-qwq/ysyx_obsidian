#c #操作系统
### 今日解决疑问:

stack中预留一大片是多大, 这个大小是谁给定的? 

>根据操作系统, Linux通常给8MB. 

栈帧的返回地址, 保存寄存器都是什么, 这是属于text,data还是heap, stack? 

>返回地址是当前被调用函数后面的下一条指令, 用于离开函数时返回原先的指令顺序

stack的栈顶是什么, 如果这个栈是指代进程的栈, 那这个, 栈顶是高地址的第一个位置? 所以这寄存器里存了很多个不同进程的栈顶指针是吗,?

>这里的栈顶是数据结构概念里的栈顶, 实际上由于stack应该是向低地址生长的, 因此指向低地址. 由于一个内核cpu只负责一个进程, 所以只有一个指针.

--- 

### 今日工作总结：

- 理解程序究竟如何通过操作系统运行